The submission URL is `/` on the deployed Next app. `/architecture` is the
supporting evidence route.

The architecture route renders semantic HTML with no CDN, webfonts, or remote
images. On desktop it also shows an SVG summary; download it from the page for
the same offline vector.

The older self-contained file is still here:

```bash
open docs/architecture/morrow-architecture.html
```
